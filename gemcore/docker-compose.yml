services:
  gemcore:
    build: .
    ports:
      - "4300:4300"
    environment:
      GEMCORE_PORT: 4300
      GEMCORE_DATA_DIR: ./data
    volumes:
      - gemcore-data:/app/data
volumes:
  gemcore-data:
