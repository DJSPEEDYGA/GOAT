class CaptureDevice{constructor(meta){this.meta=meta;this.stream=null}async open(constraints={video:true,audio:false}){if(!navigator?.mediaDevices?.getUserMedia)throw new Error('Camera API unavailable');this.stream=await navigator.mediaDevices.getUserMedia(constraints);return this.stream}stop(){this.stream?.getTracks().forEach(t=>t.stop());this.stream=null}capabilities(){const t=this.stream?.getVideoTracks?.()[0];return t?.getCapabilities?.()||{}}async apply(settings){const t=this.stream?.getVideoTracks?.()[0];if(!t)throw new Error('No active camera');await t.applyConstraints({advanced:[settings]});return t.getSettings()}}window.GemCoreCapture={CaptureDevice};