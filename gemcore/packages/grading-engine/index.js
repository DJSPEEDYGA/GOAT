const RUBRIC='gemcore-preactive-0.2';const clamp=n=>Math.max(1,Math.min(10,n));function grade(input){const c=input.categories||{},keys=['centering','corners','edges','surface','printQuality'];for(const k of keys)if(typeof c[k]!=='number')throw new Error('Missing verified category '+k);const vals=keys.map(k=>clamp(c[k])),avg=vals.reduce((a,b)=>a+b,0)/vals.length,low=Math.min(...vals),major=(input.observations||[]).some(o=>o.reviewerDisposition==='confirmed'&&o.severity>=.85);let final=Math.min(avg,low+.6);if(major)final=Math.min(final,7);return{rubricVersion:RUBRIC,grade10:Math.round(final*10)/10,conditionIndex:Math.round(final*100),categories:Object.fromEntries(keys.map((k,i)=>[k,vals[i]])),guardrails:{lowestCategory:low,majorDefect:major}}}module.exports={grade,RUBRIC};