{
  "name": "goat-investor-epk",
  "version": "1.0.0",
  "private": true,
  "description": "Gated GOAT Royalty Living EPK — password + TOTP 2FA + IP/login audit + owner approval gate. Launches approved partners/investors to allowed apps only; exposes no source code or IP.",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "add-investor": "node scripts/add-investor.js",
    "lint": "node --check server.js && for f in src/*.js; do node --check \"$f\"; done"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.6",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "jsonwebtoken": "^9.0.2",
    "otplib": "^12.0.1",
    "qrcode": "^1.5.3"
  }
}
